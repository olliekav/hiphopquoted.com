<!DOCTYPE html>
<!--[if IE 9 ]><html lang="en" class="ie9 no-js"><![endif]-->
<!--[if (gt IE 9)|!(IE)]><!--> <html lang="en" class="no-js"> <!--<![endif]-->
  <head>
    <meta charset="utf-8">
    <title><%= page_title %></title>
    <meta name="description" content="<%= page_description %>"/>
    <meta content="Hip-Hop, Hip-Hop Quotes, Hip-Hop Quoted, Hip-Hop Lyrics, Rap Quotes, Rap, Rap Quoted, Rap Lyrics, CSS3, HTML5, Art Direction, Typography, Fonts" name="keywords"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width">
    <link href="/images/favicon.ico" rel="shortcut icon"></link>
    <script>(function(h){h.className = h.className.replace('no-js', 'js')})(document.documentElement)</script>
    <%= stylesheet_link_tag "site.css" %>
    <%= yield_content :stylesheet %>
    <!--[if lt IE 9]>
      <%= javascript_include_tag("html5shiv.js") %>
    <![endif]-->
    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
      ga('create', 'UA-10920756-1', 'auto');
      ga('send', 'pageview');
    </script>
  </head>
  <body id="hiphopquoted" class="page-<%= page_classes %><%= ' content-page' if content_page? %>">
    <%= partial "header" %>
    <main id="main" role="main">
      <%= yield %>
    </main>
    <%= partial "footer" %>
    <%= javascript_include_tag("application.js") %>
    <%= yield_content :javascript %>
    <% if content_page? %>
      <%= partial "font_javascripts", :locals => { :font_id => "lsk1lvz", :include_big_text => false, :include_lettering => false, :script => false } %>
    <% else %>
      <%= javascript_include_tag("/javascripts/classList.js") %>
      <%= javascript_include_tag "/javascripts/iTunesPreviewPlayer.js" %>
      <script>
        var track = "<%= current_page.data.track %>";
        var playerCountry = "<%= current_page.data.player_country %>";
        var playerColor = "#<%= current_page.data.player_color %>";
        var playerAttribute = "<%= current_page.data.player_attribute %>";
      </script>
    <% end %>
    <script>
      $(window).bind("load", function() {
        $('html').addClass('js-load');
        <% unless content_page? %>
          var player = new iTunesPreviewPlayer(track);
          var audio = document.getElementsByTagName('audio');
          document.getElementById("audio-clip").appendChild(player.DOMElement());
        <% end %>
      });
      if( $(window).width() <= 768 ) { 
        setTimeout(function(){
          window.scrollTo(0,0);
        },1000)
      }
    </script>
  </body>
</html>